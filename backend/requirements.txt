# Géoportail Agricole CAR-GON — Dépendances Python
# Stack : Django + GeoDjango + DRF + PostGIS + Celery + GDAL

# Framework web
Django>=4.2,<5.0

# Base de données PostGIS
psycopg2-binary>=2.9

# API REST
djangorestframework>=3.14
djangorestframework-gis>=1.0
djangorestframework-simplejwt>=5.3.0
django-filter>=24.0
drf-spectacular>=0.27

# Configuration
django-environ>=0.11

# CORS
django-cors-headers>=4.3.0

# Tâches asynchrones (traitements > 5s)
celery>=5.3
redis>=5.0

# Géospatial / traitements SIG
# GDAL est installé via apt-get (python3-gdal)

# Génération PDF (fiche synoptique, exports)
reportlab>=4.0
WeasyPrint>=60.0

# Utilitaires
Pillow>=10.0
